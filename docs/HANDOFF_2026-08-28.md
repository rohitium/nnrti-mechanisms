# Handoff — 2026-08-28

> **SUPERSEDED IN PART — see the status block below (updated 2026-08-30).**
> Sections 1–4 remain the record of *why* things are as they are; the open
> items in §5–§8 have largely been closed.
>
> ## Status as of 2026-08-30 evening
>
> **MM/GBSA: DONE.** Final panel is 100 evenly spaced frames, **no filtering**,
> minimised to convergence (2000 iterations), CPU double precision. 60 runs,
> zero failures. Mean SEM **0.53** (published protocol was 1.07; the screened
> intermediate 0.59) — *keeping every frame turned out to be more precise than
> screening*, because the earlier apparent advantage of screening was an
> artifact of an unconverged 100-iteration minimisation. Table 2, Supplementary
> Table 3, plots and classification metrics all rebuilt. Manuscript change list:
> `manuscript/CHANGES_2026-08-30_final_mmgbsa.md`.
>
> **G190E FEP: partially resolved.** 20 ns equilibration gave
> **+0.99 ± 1.63** (from +2.00 ± 1.77). The lever worked on its own terms —
> sd(holo−apo hysteresis) 7.39 → 4.52 — but σ_DDG only moved 3.06 → 2.82
> because one unit carries the variance. The work distributions localise it to
> **holo replicate 2's λ=1 (mutant) endpoint ensemble**; see
> `scripts/fep_pmx/RUNBOOK_G190E_SEM.md` §"Lever C".
>
> **In flight (submitted 2026-08-30):**
> - `41352553` — holo rep 2 λ=1 equilibration re-run, 7 elements total
>   (1 equil → 1 extract → 5 reverse switch bundles), submitted stage-by-stage
>   with **no `afterok` chaining**.
> - `41352310/31/35` — apo G190E MD ×3 at a **48 h** wall (the `gpu` partition
>   caps at 2 days regardless of QOS). These had truncated twice at 12 h; they
>   are the only thing blocking Lever B.
>
> ### G190E Lever C — live state (2026-08-31, switches submitted)
>
> | job | stage | status |
> |---|---|---|
> | `41352553` | holo rep2 λ=1 equil, 20 ns | **COMPLETED** 6h17m; `equil.gro` = 169,840 lines ✓ |
> | `41442538` | holo rep2 λ=1 extract | **COMPLETED** 32m; **100/100 snapshots verified**, uniform size, spanning 100–16,181 ps ✓ |
> | `41458114_[0-4]` | holo rep2 reverse switches, 5 bundles × 20 | **SUBMITTED / PENDING**, ~3 h once started |
> | `41352310` / `41352335` | apo G190E MD reps 1, 3 | **COMPLETED 100 ns**, status ok, 50,000,000 steps |
> | `41352331` | apo G190E MD rep 2 | RUNNING, ~17 h of a 48 h wall |
>
> **Next actions, in order:**
> 1. When `41458114` finishes, analyse (the stale `analysis/` for this unit is
>    ALREADY archived — no `--force`, no further archiving):
>    ```
>    python3 scripts/fep_pmx/combine_neq.py --targets G190E --replicates 3
>    python3 scripts/fep_pmx/qc_neq.py --legs wt_to_G190E --replicates 3
>    ```
> 2. **Rebuild the full 19-genotype panel** — `--targets G190E` alone truncates
>    `panel_ddg.csv` to one row (this happened on 2026-08-30). Full list is in
>    `RUNBOOK_G190E_SEM.md`.
> 3. Back up the leg once the queue is clear:
>    `LEGS="wt_to_G190E" bash scripts/sherlock/backup_fep_raw.sh`
>    (`equil.trr` alone is 8.1 GB for this one unit, and /scratch purges at 90 days.)
>
> **The number that decides everything:** holo rep 2's *reverse* mean work,
> previously **−55.43** against −64.07 (rep 1) and −62.56 (rep 3). Near −63 →
> the original was a fluke, σ_DDG collapses, G190E ≈ **−0.6 ± 0.1** with its
> sign flipped. Near −55 again → that ensemble is real, σ_DDG ≈ 2.8 stands, the
> leg needs n ≈ 8, and G190E is reported unresolved with a reason. Both are
> publishable; the pre-repair state was not.
>
> **Environment gotcha:** a fresh Sherlock login has no `(pmx)` prefix.
> `source scripts/sherlock/activate_pmx_env.sh` before any `submit_p0_neq.sh`
> or `combine_neq` — login python3 is 3.6 and `GMXLIB` must point at pmx's
> `mutff`.
>
> ### Manuscript work landed 2026-08-31
>
> `manuscript/discussion-expansion-2026-08-31.md` — a substantially expanded
> Discussion in response to Rohit's request for mechanism-level treatment of the
> significant results. Six paragraphs: overall performance with the
> classification metrics; **why the susceptible genotypes are susceptible**
> (K103N's backbone H-bond preserved at 3.08 ± 0.06 Å vs WT 2.97 ± 0.01, and in
> all four K103N backgrounds within 0.23 Å; Y181C costing nothing because Tyr188
> carries the anchoring alone; G190A unperturbed on every coordinate); the
> **position-190 series**; the existing Y188L/V106A mechanisms; a **contrast with
> ETR/RPV** (DAPY torsional flexibility vs DOR's constrained scaffold plus a
> main-chain H-bond — the same choice explains Y181C at 1.4-fold and Y188L at
> 149-fold); and **why the calculations fail where they do** (V106I over-called on
> real but tolerated strain; A98G+F227C and K103N+M230L missed because residues 98
> and 230 sit 9.1 Å and 7.3 Å from DOR and no interface coordinate is degraded).
> Three `[cite]` markers flag literature claims needing references.
>
> **Two data corrections found while writing it:**
> 1. **WT chlorocyanophenyl ring burial.** The draft says `45.3 ± 2.4` (WT) and
>    `34.2 ± 1.1` (Y188L) in **two places**. Correct values are **45.9 ± 1.3** and
>    **34.6 ± 0.9** — what `mechanism_summary.csv` holds today. The old pair is not
>    reproducible from the committed `mechanism_coordinates.csv` under any
>    aggregation (replicate SEM, replicate SD, pooled frames, or the interpolated
>    trace) and was carried forward verbatim from the 08-26 draft. "A loss of
>    roughly a quarter" survives the fix; Figure 3A is unaffected.
> 2. **G190S's Tyr188 interplanar angle is bimodal, not shifted.** 27.4 ± 5.3° is a
>    frame average over two populations — 37% of frames still coplanar (<15°) and
>    32% rotated past 40° *and* withdrawn from the ligand (centroid 4.23 → 5.05 Å).
>    The SEM reflects real replicate heterogeneity (34.2/17.0/30.9°), not
>    imprecision. Relatedly **G190E does not disrupt the stack at all** (12.1 ± 0.9°,
>    tighter than WT's 13.4 ± 0.4°) — so position 190 is *not* a severity gradient;
>    it is three different mechanisms. Table:
>    `results/analysis/mechanisms/y188_interplanar_angle_190series.csv`.
>
> Note `compute_mechanism_coordinates.py` was never run for A98G+F227C,
> V106I+F227C, Y318F or F227C; statements about A98G+F227C use the key-contact
> tables instead. Frame counts per replicate are unbalanced (139–540; WT rep 1 has
> 500 vs 180 for reps 2–3), so replicate-mean-then-average is the correct
> aggregation and pooling frames would bias toward WT rep 1.
>
> **Still to do:** JCIM abstract (must become unstructured; deferred until
> G190E settles); two hand fixes in Word (Figure 3's caption is embedded
> mid-paragraph, Table 1's title is inside the table header); the repo refactor
> (`docs/REFACTOR_PLAN.md`, staged for when queues are quiet).
>
> **Also landed since 2026-08-28:** `REPRODUCE.md`; `envs/nnrti-prep.yml` (the
> analysis environment was captured nowhere); `scripts/manuscript/apply_acs_template.py`
> (JCIM formatting); `src/analysis/cli/classification_performance.py` (MCC/AUC —
> FEP 0.50/0.78 against MM/GBSA 0.03/0.69); `manuscript/methods-statistical-analysis.md`.

State of play at the end of a long working session. Written so a fresh agent (or
Rohit after a break) can pick up without re-deriving anything. Read
[`REFACTOR_PLAN.md`](REFACTOR_PLAN.md) and
[`../scripts/fep_pmx/RUNBOOK_G190E_SEM.md`](../scripts/fep_pmx/RUNBOOK_G190E_SEM.md)
alongside this.

Target journal: **JCIM**. Working draft:
`manuscript/post-feedback-from-atanu/DorDRM-MD-09-02-26.docx` (note: *09-02*, not
the 08-26 file most tooling still defaults to).

---

## 1. THE CRITICAL FINDING — do not score MM/GBSA on OpenCL

**Single-precision OpenCL silently produces catastrophic MM/GBSA energies on
frames containing severe atomic overlaps.** Two of 32 tested overlap frames blew
up:

| run | frame | OpenCL | CPU (double) |
| --- | ---: | ---: | ---: |
| Y188L rep1 | 175 | **+289,229,277** | −45.26 |
| Y318F rep2 | 138 | **+12,050** | −65.89 |

Mechanism: frame 138 has a hydrogen–hydrogen contact at **0.216 Å**
(LYS100:HG3 ↔ ligand H1x). The r⁻¹² gradient (~10¹³) overflows single precision,
`LocalEnergyMinimizer` aborts before its first step, and the energy is returned
unminimised. The tell is that the value is **identical to the cent at 100, 500,
2000 and 10000 iterations** — the minimiser is not running at all. Heavy-atom
geometry in that frame is fine (1.97 Å min) and ligand bond lengths are normal;
only the hydrogen placement is broken, consistent with the DCD-writing artifact
described in `results/analysis/binding_energy/MMGBSA_METHOD_AND_RECOMPUTE.md`.

Even excluding blow-ups, OpenCL disagrees with CPU by up to **3.67 kcal/mol** on
overlap frames (mean 0.37), versus 0.059 kcal/mol on clean frames.

**Why the earlier platform validation missed it:** it compared CPU and OpenCL on
six runs drawn from the *contact-screened* frame set, so it contained no
pathological frames — precisely the population that later stopped being
excluded. `platform_validation_cpu_vs_opencl.csv` is therefore valid only for the
screened protocol. Any future platform check must include overlap frames.

**RESOLVED 2026-08-28 22:51** — canonical panel relaunched on CPU at **2000
minimisation iterations** (was 100), no frame screening, 12 workers, output
`.checkpoint_mmgbsa_cpu2000_2026-08-29.csv`. ~20-25 h.

The iteration sweep (`minimisation_convergence_cpu.txt`) showed the 100-iteration
cap was the larger problem: overlap frames were not converged, and the apparent
"7 kcal/mol residual" was mostly unfinished minimisation, not a property of
those frames.

| iters | overlap | clean | gap |
| ---: | ---: | ---: | ---: |
| 100 | -50.83 | -63.44 | **12.6** |
| 500 | -64.45 | -64.61 | 0.16 |
| **2000** | **-64.15** | **-65.45** | 1.30 |
| 5000 | -64.21 | -65.56 | 1.35 |

Both converge by 2000 (stable to ~0.1 kcal/mol out to 5000). The residual
~1.3 kcal/mol at convergence is physical -- those frames sit ~2 A from the clean
mean DOR pose -- i.e. real ensemble breadth, which is the point of keeping them.

**Action (superseded, kept for the record): the canonical panel must be rescored on CPU.** ~6000 frames at ~33
s/frame; with 10–12 workers expect ~24–27 h wall.

```bash
OPENMM_PLATFORM=CPU OPENMM_CPU_THREADS=1 PYTHONPATH=. \
python -m src.analysis.cli.compute_mmgbsa_safe \
  --frame-sampling even --snapshots 100 --discard-fraction 0.25 \
  --snapshot-relaxation unrestrained --relaxation-iterations 2000 \
  --workers 12 --force \
  --output results/.checkpoints/.checkpoint_mmgbsa_cpu2000_2026-08-29.csv
```

Note `--workers 4` on OpenCL crashed the pool once (a worker died natively,
poisoning `ProcessPoolExecutor` and failing all 18 pending tasks at once); 2
workers was stable. On CPU, workers are independent — but re-check.

---

## 2. MM/GBSA protocol — where it landed and why

Three protocols now exist. Comparison:
`results/analysis/binding_energy/protocol_comparison_three_way.csv`.

| protocol | frames | filtering | mean SEM | max | >1.0 |
| --- | --- | --- | ---: | ---: | ---: |
| terminal-20 (published) | last 20 | contact screen | 1.07 | 2.55 | 9/18 |
| even-100 screened | 100 even | contact screen | **0.59** | 1.31 | 3/18 |
| even-100 unfiltered | 100 even | none | pending CPU rerun | | |

**Two independent changes, do not conflate them.**

**(a) Sampling window — settled, keep.** The terminal-20 protocol scored the
final ~10 ns of each 100 ns run. A variance decomposition shows only a median
**4%** of between-replicate variance is 20-frame noise, so more terminal frames
would not help. A five-window scan across four systems shows the replicate spread
collapses when the whole post-equilibration region is sampled (G190E 4.41 →
1.10, Y188L 3.80 → 0.64 kcal/mol): adjacent terminal frames are strongly
autocorrelated, so each replicate mean landed on whatever excursion its run ended
in. Evidence: `window_scan_2026-08-28.csv`.

Gated on the ligand staying in one binding mode
(`screen_mmgbsa_pose_stability.py`): DOR RMSD to reference 1.27 Å mean, spread
about each run's own mean 0.14 Å median. One of 60 runs flagged (G190S rep 3,
3.17 Å max but 0.22 Å self-spread — displaced yet stable).

**(b) Contact screen retired — Rohit's decision, has a cost.** Frames are no
longer discarded; overlaps are handled by minimisation. Rationale: the artifact
guarded against is ~3300 kcal/mol, so a ~7 kcal/mol residual after minimisation
is immaterial. Verified the minimiser does repair contacts (0.92 Å → 3.01 Å at
100 iterations, 0.44 Å geometry drift). Evidence:
`contact_screen_retirement_test.csv`, `minimisation_drift_test.csv`.

The cost is real and concentrated in heavily-flagged runs. Y188L is the clean
example:

| rep | ΔG | per-frame σ | frames the screen flagged |
| --- | ---: | ---: | ---: |
| 1 | −57.55 | 4.65 | **122/180 (68%)** |
| 2 | −61.01 | 2.04 | 0 |
| 3 | −62.57 | 2.04 | 10 (6%) |

Y188L SEM goes 0.55 (screened) → ~1.48 (unfiltered), because rep1 — two thirds
of whose frames were being removed — now pulls away from its siblings. Flagged
frames sit ~2.0 Å from the clean mean DOR pose versus ~0.7 Å for clean frames,
so including them genuinely widens the sampled ensemble.

**Open question for the author:** are those frames sampling you want in the
average, or corruption you want out? The evidence is genuinely two-sided —
the pre-minimisation energies are thermodynamically unreachable at 300 K
(suggesting artifact), but post-minimisation they are physical structures in a
displaced pose (suggesting sampling). This is a scientific call, not a technical
one. It was decided in favour of keeping them; revisit only with new evidence.

### Still to do on MM/GBSA
- [ ] Rescore the panel on **CPU** (§1). The OpenCL unfiltered run in
      `results/analysis/binding_energy/allframes100/` has a corrupted Y318F rep2
      and must not be promoted.
- [ ] Y318F rep2 was being recomputed on CPU into
      `.checkpoint_mmgbsa_allframes_2026-08-28.csv` when the session ended
      (59/60 rows present). A full CPU rerun supersedes this anyway.
- [ ] Promote → `rebuild_binding_energy_sources` → `build_table_2` (pass
      `--docx .../DorDRM-MD-09-02-26.docx`) → `build_supplementary_table_3` →
      plots.

---

## 3. Manuscript — what still needs changing

The 09-02 draft already has some updates applied by hand from the **screened**
even-100 numbers. Those will shift again after the CPU rerun. **Do not hand-edit
further numbers until the CPU panel is final.**

Already applied in 09-02: abstract counts (7 of 9 resistant; R² = 0.02, p = 0.60;
R² = 0.26, p = 0.03), the Methods sampling sentence ("100 evenly spaced snapshots
across the final 75 ns"), the 7-of-9 Resistant sentence, and the K103N /
K103N+M230L negative-energy sentence.

Still stale in 09-02 (values below are from the *screened* run — recompute):
- **Table 2 itself is untouched.** Rebuild it.
- "A98G+F227C (1.09 ± 1.35) and V106A+P225H (1.16 ± 0.86) are positive but not
  resolved above the 0.5 kcal/mol threshold" — both now resolve.
- "K103N+L100I and K103N+P225H … weak impact" — K103N+P225H is now strong.
- "Out of the 12 genotypes … 9 involve ∆∆EvdW … remaining 3 ∆∆EGB" → 14/12/2.
- "∆∆Eelec … 10 out of the 18" → 9.
- Mechanisms: "∆∆EvdW = 3.93 ± 1.39" → 2.85 ± 0.46. (Y188L remains the largest
  vdW penalty *among Resistant* genotypes; across the whole panel G190S is
  fractionally higher, so do not loosen that qualifier.)
- Discussion: "6 out of 9" → 7 of 9; "R² = 0.25" → 0.26.
- **Discussion paragraph that is now factually wrong:** "∆∆ETotal values
  typically showed larger error bars than ∆∆Gbind … intrinsic noise in the
  MM/GBSA approach … mitigated by (i) more replicates (ii) freezing or
  truncating atoms far from the binding pocket." Under even-100, MM/GBSA errors
  are *smaller* than FEP's (0.59 vs 0.72 mean SEM, smaller in 10 of 18
  genotypes), and the cause was the sampling window, not the method. Rewrite.

### JCIM formatting (task 4, not started)
Verified against the live guidelines (July 2026):
- **Abstract must be unstructured**; guideline asks for 3–4 sentences. The draft
  is **289 words under Background/Methods/Results/Conclusion**. Needs an
  editorial rewrite — deferred until numbers are final.
- TOC graphic and keywords: required for Perspectives/Reviews, **not** Articles.
- Template `manuscript/post-feedback-from-atanu/acstemplate_msw2011_mac.dotx` is
  committed. Style map: `BA_Title`, `BB_Author_Name`, `BC_Author_Address`,
  `BI_Email_Address`, `BD_Abstract`, `TA_Main_Text`, `VD_Table_Title`,
  `TC_Table_Body`, `VA_Figure_Caption`, `TF_References_Section`.
- Approach: apply ACS styles **to** the draft (unzip → edit `document.xml` →
  rezip), *not* rebuild from the template — the draft carries 4 inline images
  (3 EMF figures + an equation) and 2 tables that must survive.

---

## 4. Getting every SEM below 0.5 kcal/mol

Costed 2026-08-28. `SEM = σ/√n`, so `n = 4σ²` — the payoff from reducing σ is
quadratic, from adding replicates only linear. **Reduce σ first.**

### MM/GBSA — 67 extra replicates (~2,500 GPU-h)
Eight of 18 genotypes already qualify. Worst offenders and required n:

| genotype | σ | SEM now | n needed | extra |
| --- | ---: | ---: | ---: | ---: |
| L100I+K103N | 2.26 | 1.31 | 21 | 18 |
| V106M | 2.19 | 1.26 | 20 | 17 |
| G190S | 1.85 | 1.07 | 14 | 11 |
| V106I | 1.69 | 0.97 | 12 | 9 |
| V106A+F227L | 1.31 | 0.76 | 7 | 4 |
| G190E / K103N+M230L / G190A / Y188L / A98G+F227C | | | | 1–3 each |

Checked: **not** outlier-driven. L100I+K103N's replicates are +2.51 / −1.93 /
+1.07 — a genuine 4.4 kcal/mol spread; dropping any one does not rescue it. So
replicates are the only lever here. New replicates come from
`src/structure_prep/preparation.py --replicates N`, which generates rep_04+
idempotently with fresh jitter seeds and leaves reps 1–3 untouched.

### FEP — 138 extra leg-replicates, and the naive route is impossible
Solved over the shared-leg structure (`wt_to_K103N` feeds four genotypes):

| leg | σ_ΔΔG | n needed | extra |
| --- | ---: | ---: | ---: |
| wt_to_G190E | 3.06 | **38** | 35 |
| V106I_to_V106I_F227C | 1.56 | 20 | 17 |
| V106A_to_V106A_P225H | 1.74 | 16 | 13 |
| wt_to_Y181C | 1.84 | 14 | 11 |
| wt_to_F227C | 1.41 | 14 | 11 |
| wt_to_V106A | 0.89 | 13 | 10 |
| wt_to_V106I | 1.28 | 13 | 10 |
| V106A_to_V106A_F227L | 1.49 | 12 | 9 |
| V106A_to_V106A_L234I | 1.68 | 15 | 12 |
| K103N_to_K103N_P225H | 1.31 | 9 | 6 |
| K103N_to_L100I_K103N | 1.11 | 7 | 4 |

≈1,100–1,650 GPU array elements. **The blocker is seeding, not switching:** each
leg-replicate needs an independent MD conformation of all four end structures.
G190E at n=38 would need 35 more each of WT-holo, WT-apo, G190E-holo,
G190E-apo — 140 extra 100 ns runs, ~5,300 GPU-h for one leg. Not viable.

**Therefore: σ-reduction first.** The 20 ns equilibration campaign now running is
exactly this test. If it takes G190E's σ from 3.06 to ~1.4, the requirement falls
from n=38 to n=8 — the difference between impossible and a weekend.

### Shared bottleneck
Extra MD replicates serve **both** campaigns: the same rep_04+ runs feed MM/GBSA
and seed FEP legs. Plan them once. Apo runs are needed for FEP seeding but not
for MM/GBSA.

---

## 5. Jobs in flight at session end (Sherlock)

| job | what | state |
| --- | --- | --- |
| `41149155_[0-11]` minus `_3` | G190E equil, 20 ns, 11 units | RUNNING ~2h45m |
| `41155898_0` | G190E equil repair, holo rep2 λ1 | RUNNING ~1h37m |
| `41123641/45/46` | apo G190E MD ×3, 100 ns | RUNNING ~7h49m |
| `41158304` | FEP tier-1 backup | COMPLETED, verified (see §6) |

**Next FEP step:** when all 12 equil units finish, run

```bash
bash scripts/fep_pmx/resume_g190e_chain.sh
```

which audits equil (refusing to proceed unless 12/12), checks for truncated
`equil.gro`, and chains extract → switch with per-batch task-id files.
`AUDIT_ONLY=1` to look without submitting. `41149158`/`41149159` were cancelled
when equil element 3 died and must be resubmitted this way.

**Known operational gotchas hit this session** (all now in the runbook):
- `gpu` QOS caps **100 submitted jobs**; array elements count individually. 120
  switch elements can never be submitted — hence 20 snapshots/task (60 elements).
- Switch bundling default (50/task) gives ~21 h elements, over the 12 h wall.
- Transient `cudaErrorLaunchFailure` killed one equil element after 9200 steps.
  Recovery: one-row manifest + `EXCLUDE_NODES` + resubmit; recipe in the runbook.
- `prepare_neq` prints the same line whether or not `--force` did anything. Only
  the gate distinguishes.

---

## 6. Data safety — done and outstanding

**Done:**
- MD data mirrored to `$GROUP_HOME/nnrti-mechanisms/md_runs/` — 828 files,
  verified byte-identical by `rsync --checksum` on both ends. Manifest committed
  as `manifests/md_runs_manifest_2026-08-28.txt`.
- `.gitignore` hole closed: the `fep_pmx/legs/**` heavy-data whitelist did not
  cover `_archive/`, so archiving a run moved ~65 GB of `dgdl.xvg`/`equil.trr`
  into a git-addable state. Now mirrored for `_archive/`.
- FEP tier-1 backup **complete and verified** (job 41158304): four K103N legs,
  2652 entries each, sha256 recorded, 4.3 G -> 4.2 G in
  `$GROUP_HOME/nnrti-mechanisms/fep_raw/2026-08-28/`. The interrupted
  login-node attempt left `wt_to_K103N_tier1.tar.gz.unverified`, correctly
  quarantined by the manifest-entry check rather than skipped as complete --
  the fix works.

**Outstanding:**
- [ ] Commit `BACKUP_MANIFEST.csv` as `manifests/fep_raw_backup_2026-08-28.csv`.
- [ ] Back up `wt_to_G190E` **after** the campaign drains:
      `LEGS="wt_to_G190E" bash scripts/sherlock/backup_fep_raw.sh`. Includes the
      1200 archived `dgdl.xvg` from the 5 ns arm.
- [ ] Tier 2 (endpoint trajectories, ~250 GB) — **declined**; tier 1 is the
      scientific record. Revisit only if re-running switches becomes likely.

**Context that shapes all of this:** the full MD trajectories were lost to a
`/scratch` 90-day purge, and 14 of 19 legs' raw FEP data to a `git clean -fd` on
2026-08-13. What survives: stripped analysis DCDs (now backed up), the committed
`integ_*.dat` work values (228 files — every ΔΔG in the paper is recomputable
from these), and raw switch data for 5 legs on scratch.

---

## 7. Open decisions

1. **Contact screen** — keep frames (current) or restore the screen? See §2(b).
   Costs ~3× on the SEM of heavily-flagged genotypes.
2. **Repo refactor** — package rename to `src/nnrti/` + `pyproject.toml` was
   agreed as best practice, staged for after the queues drain. See
   `REFACTOR_PLAN.md`. Archive goes to `$GROUP_HOME`, not git.
3. **`.git` is 1.8 GB**, mostly blobs from directories that no longer exist on
   disk (`dor_key_contacts_timeseries_by_mutation` 81 MB × 3, `feature_combo_*`
   21.9 MB × 2). A `git filter-repo` + force-push + fresh Sherlock clone would
   fix it. Worth doing before the JCIM deposit; must not be done while jobs run.
4. **Deposit scope** — `REPRODUCE.md` must start from the stripped DCDs and the
   `integ_*.dat` work values, since the raw MD and most raw FEP no longer exist.
   State that honestly rather than implying full reproducibility from scratch.

---

## 8. Small stuff found and not yet fixed

- `plot_binding_energy_variability.py` filters `mutation_ddg_summary.csv` on a
  `category` column that no longer exists → silently writes nothing, leaving a
  stale PNG. Should fail loudly, or be retired (`ddg_vdw_std` was partly an
  artifact of terminal-window sampling). Not cited in the manuscript.
- `run_analysis.sh` runs none of the scripts that build manuscript artifacts; 61
  of 82 CLIs are referenced by no script or doc. See `REFACTOR_PLAN.md`.
- `combine_neq.py`'s `OMIT_MAIN_TEXT` still hardcodes `{"K103N", "G190E"}` from
  when both had SEM > 2. K103N is now 0.23. Revisit when G190E lands.
- Sherlock git needed `git config push.default simple` (old git defaulted to
  `matching`, pushing stale branches).
- `results/analysis/fep_pmx/neq_switch_g190e_e20_task_ids_chunk001.txt` is a
  stale leftover from the abandoned 10-snapshot/task bundling. Untracked and
  inert; move to `_archive/` rather than deleting if it becomes annoying.
